/*
Input: list of ships objects, each with pre-populated 'intactOffsets', drop mins and maxes, and captain (player #)
Output: 2D array of 'gridSquare' objects, each with it's 'val' and 'shiplist' populated
*/
export function placeShips(shipList){
    alert("You have called me and passed in " + shipList);
}